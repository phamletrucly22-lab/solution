# Nest API (솔루션·총판·관리자 공통)
NEXT_PUBLIC_API_URL=http://localhost:4001/api
# agent.<solution-domain> 에서 host 기준으로 플랫폼이 자동 식별됩니다.
# nginx 로 /api 프록시를 붙인 운영이면 아래를 켜세요.
# NEXT_PUBLIC_USE_SAME_ORIGIN_API=true
