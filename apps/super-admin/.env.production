# mod.tozinosolution.com 등에서 브라우저가 같은 호스트의 /api 로 호출 (nginx·tunnel 이 /api → Nest 로 넘김)
NEXT_PUBLIC_USE_SAME_ORIGIN_API=true
# 빌드 타입체크·로컬 serve 시 폴백; 브라우저(비루프백)에선 same-origin 이 우선(getApiBase)
NEXT_PUBLIC_API_URL=http://127.0.0.1:4001/api
