# HTTP 전용 conf 문법 검사용 (nexus001.vip.http-only.conf 는 http{} 없이 sites-enabled 에만 include)
#
#   sudo nginx -t -c /절대경로/tosino/deploy/nginx/nexus001.vip.http-only.standalone-nginx-test.conf

events {
    worker_connections 1024;
}

http {
    include nexus001.vip.http-only.conf;
}
