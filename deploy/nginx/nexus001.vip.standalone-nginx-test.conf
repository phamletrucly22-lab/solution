# Local syntax check with: sudo nginx -t -c /ABS/PATH/tosino/deploy/nginx/nexus001.vip.standalone-nginx-test.conf
# nexus001.vip.conf must be inside http{}; using it alone as -c causes "upstream directive is not allowed here".

events {
    worker_connections 1024;
}

http {
    include nexus001.vip.conf;
}
